version: "3.9"

services:
  experiment:
    build:
      context: .
      dockerfile: Dockerfile
    image: column_generation
    command: python3 new_flags_experiment.py
    volumes:
      - /home/danlawand/.gurobi/gurobi.lic:/opt/gurobi/gurobi.lic:ro
      - /home/danlawand/causal-reasoning/resultados:/code/outputs
    # restart: unless-stopped
    container_name: experiment_container